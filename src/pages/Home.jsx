import MainContent from '../components/MainContent';

const Home = () => {
    return (
        <div className=" bg-blue-50">
            <MainContent/>
        </div>
    );
};

export default Home;
