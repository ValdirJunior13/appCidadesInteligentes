import MainContent from '../components/MainContent';

const Home = () => {
    return (
        <div className="min-h-screen bg-blue-50">
            <MainContent/>
        </div>
    );
};

export default Home;
